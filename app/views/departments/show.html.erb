<div class="card">
  <div class="card-body">
    <h5 class="card-title">Department Details</h5>
    <p class="card-text"><strong>Department Name:</strong> <%= @department.name %></p>

    <div class="text-center">
      <%= link_to 'Edit', edit_department_path(@department), class: "btn btn-primary" %>
      <%= link_to 'Back', departments_path, class: "btn btn-secondary" %>
    </div>
  </div>
</div>
